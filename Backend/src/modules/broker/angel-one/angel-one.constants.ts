/**
 * Angel One's publicly documented instrument master JSON URL (SmartAPI docs).
 * Not exercised against the real endpoint in this environment — verify before
 * enabling Live trading. This constant lives only inside the Angel One
 * adapter; nothing outside modules/broker/angel-one knows it exists.
 */
export const ANGEL_ONE_INSTRUMENT_MASTER_URL =
  'https://margincalculator.angelone.in/OpenAPI_File/files/OpenAPIScripMaster.json';
