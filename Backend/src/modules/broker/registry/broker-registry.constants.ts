/** Multi-provider token — same Strategy-registry idiom as `SCHEDULED_JOBS`/`HEALTH_INDICATORS`/trade-validation's `RuleRegistry`. Each provider contributes one `BrokerDefinition`. */
export const BROKER_DEFINITIONS = Symbol('BROKER_DEFINITIONS');
