/**
 * LONG: profits as price rises (entry via BUY, exit via SELL).
 * SHORT: profits as price falls (entry via SELL, exit via BUY).
 */
export enum TradeDirection {
  LONG = 'LONG',
  SHORT = 'SHORT',
}
