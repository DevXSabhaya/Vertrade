NODE_ENV=
PORT=3000
MONGODB_URI=

# DhanHQ v2 credentials — never commit real values. Only required when
# TRADING_MODE=LIVE below; a Paper-only deployment boots fine with all of
# these left blank.
#
# DHAN_CLIENT_ID: your Dhan trading account's Client ID (e.g. 1000000132).
# DHAN_ACCESS_TOKEN: a 24-hour JWT generated manually in the Dhan web
#   console (My Profile -> Access DhanHQ APIs). Dhan has no password/TOTP
#   login flow to automate — this token IS the credential; treat it exactly
#   like a password. The app automatically renews it once per session via
#   DhanHQ's real POST /v2/RenewToken endpoint (BrokerSessionManager calls
#   DhanBrokerAuth.refresh()); you only need to regenerate this value by
#   hand if the deployment stays down for more than 24h.
# DHAN_API_KEY: NOT required, even in LIVE mode. Per DhanHQ's official docs,
#   app_id/app_secret are only used by its OAuth "consent" flow for partner
#   apps to *obtain* a token — never sent on, or needed for, any REST/
#   WebSocket call once you have a DHAN_ACCESS_TOKEN. Left here only in
#   case a future OAuth-consent-flow integration needs it.
DHAN_CLIENT_ID=
DHAN_API_KEY=
DHAN_ACCESS_TOKEN=
# Dhan's documented v2 REST/WebSocket endpoints — override only for a
# sandbox/mock Dhan-compatible endpoint, never for normal production use.
DHAN_REST_URL=https://api.dhan.co/v2
DHAN_WS_URL=wss://api-feed.dhan.co
# Passphrase used to derive the AES-256-GCM key that encrypts broker tokens at rest.
TOKEN_ENCRYPTION_KEY=

# MOCK (default) simulates ticks in-memory, no broker connection required.
# DHAN streams real market data via DhanHQ's binary WebSocket live feed.
MARKET_DATA_PROVIDER=MOCK

# MOCK (default) serves a seeded set of equities + index options (SENSEX,
# NIFTY, BANKNIFTY, CRUDEOIL) entirely in-memory, no broker connection
# required — this is what lets Paper trading resolve natural trading calls
# like "SENSEX 77200 CE" locally. DHAN downloads Dhan's real instrument
# master CSV and requires TRADING_MODE=LIVE credentials above.
INSTRUMENT_MASTER_PROVIDER=MOCK

# PAPER (default) uses the PaperExecutor (simulated fills, generous risk
# limits, no broker credentials required). LIVE uses the real DhanExecutor
# and conservative risk limits, and requires every DHAN_* credential above
# plus the TRADING_ENABLED feature flag — the app refuses to boot in LIVE
# mode if broker credentials are missing, and never silently falls back to
# PAPER.
TRADING_MODE=PAPER
# When true, the Order Queue rejects every new trade (existing trades continue).
KILL_SWITCH_ENABLED=false

# Broker Health Monitor / Scheduler configuration.
HEALTH_CHECK_INTERVAL_MS=30000
HEARTBEAT_TIMEOUT_MS=15000
# When true, overall health always reports MAINTENANCE and automatic recovery is skipped.
MAINTENANCE_MODE=false
MARKET_OPEN_TIME=09:15
MARKET_CLOSE_TIME=15:30

# Phase 12: product API authentication + paper trading account defaults.
# Secret used to sign/verify user JWT access tokens — never commit a real value.
JWT_SECRET=
JWT_EXPIRES_IN=1d
# Starting virtual balance for a newly-created paper trading account.
PAPER_TRADING_INITIAL_BALANCE=100000

# Phase 13: explicitly-trusted CORS origin(s), comma-separated for more than
# one (e.g. FRONTEND_URL=https://app.example.com,https://staging.example.com
# in production). In development, any http(s)://localhost:<port> or
# 127.0.0.1:<port> origin is ALSO allowed automatically — see
# src/core/config/cors-origin.util.ts. In production only the origin(s)
# listed here are ever allowed; there is no wildcard and no dev fallback.
FRONTEND_URL=http://localhost:5183

# Password-reset/OTP email delivery via the Gmail API (OAuth2) — sends as
# EMAIL_FROM using GOOGLE_REFRESH_TOKEN, a one-time credential obtained via
# Google's OAuth consent flow (see the Google Cloud setup guide). No SMTP, no
# third-party email API — only Google's own official client libraries
# (googleapis / google-auth-library).
#
# These four are only required in production (this deployment always sends
# real email in production, regardless of any other setting) or if you
# explicitly set EMAIL_PROVIDER=GOOGLE locally to test real sending — plain
# `npm run start:dev`/`npm test` never need them (email stays in-memory via
# the safe DEVELOPMENT provider by default).
EMAIL_PROVIDER=GOOGLE
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_REDIRECT_URI=
EMAIL_FROM=vertrade19@gmail.com
EMAIL_FROM_NAME=Vertrade
